# Resumageddon

**Resumageddon** — это консольное Python-приложение для поиска, фильтрации и сохранения вакансий с HH.ru.  
Проект написан с соблюдением принципов ООП, SOLID и покрыт тестами.  

---

## Возможности

- 🔎 Получение вакансий с [hh.ru](https://hh.ru) по ключевому слову
- 🧹 Фильтрация и сортировка по зарплате
- 📄 Сохранение вакансий в JSON-файл
- ❌ Удаление сохранённых вакансий
- 🔧 Гибкость за счёт .env-переменных

---

## 🗂 Структура проекта

```
resumageddon/
│
├── src/
│   └── resumageddon/
│       ├── api/              # Работа с внешним API hh.ru
│       ├── models/           # Классы и бизнес-логика
│       ├── storage/          # Абстракции и реализация хранилища
│       └── utils/            # Фильтрация, отладка, вспомогательные функции
│
├── tests/                    # Pytest-совместимые тесты
├── data/                     # Локальные JSON-файлы с вакансиями
├── .env                      # Переменные окружения (не коммитить!)
├── .env.example              # Пример .env-файла
├── pyproject.toml            # Poetry конфигурация
├── README.md                 # Документация проекта
└── main.py                   # Точка входа

```

---

## Установка

### Убедитесь, что у вас установлен Poetry

```
git clone https://github.com/yourusername/resumageddon.git
cd resumageddon
poetry install

```

### .env

Создайте файл .env в корне проекта и добавьте переменные:

```
HH_API_BASE_URL=https://api.hh.ru/vacancies
```

Пример файла: .env.example уже включён в проект.

---

## Запуск

```
poetry run python main.py
```

Следуйте инструкциям в терминале. Программа предложит:

- Ввести ключевое слово
- Выбрать количество вакансий
- Сохранить или очистить результаты

---
## Тестирование и покрытие

Для запуска тестов:

```
poetry run pytest
```

Для анализа покрытия:

```
poetry run coverage run -m pytest
poetry run coverage report -m
```
Покрытие тестами >70%

---

## Использовано

- Python 3.11+ 
- Poetry 
- requests 
- python-dotenv 
- pytest 
- coverage 
- flake8 / black / isort / mypy

---
# Resumageddon — учебный проект для курса backend-разработки.
## Лучшая вакансия — та, которую ты нашёл сам.